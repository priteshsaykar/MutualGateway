spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: FileUploadMS
          uri: lb://FileUploadMS
          predicates:
            - Path=/fileUpload/success
