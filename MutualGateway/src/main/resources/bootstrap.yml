   server:
     port: 9000
     
   spring:
     application:
       name: GatewayMS
     cloud:
       consul:
         host: localhost
         port: 8500
         discovery:
           enabled: true
           register: true
         config:
           enabled: true
           prefixes: config
           defaultContext: application
           profileSeparator: '::'
           format: YAML